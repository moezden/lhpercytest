/**
 * An enum representing the available selector types: CSS and XPath.
 * @readonly
 * @enum {number}
 */
const SelectorHelper = {
    CSS: 0,
    XPATH: 1
  };
  
  module.exports = SelectorHelper;
  